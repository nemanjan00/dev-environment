#!/bin/bash
# Compatibility shim: claude-docker is now `dev-docker --claude`.
# Kept so existing muscle memory / docs / self-update keep working unchanged.
exec "$(dirname "${BASH_SOURCE[0]}")/dev-docker" --claude "$@"
