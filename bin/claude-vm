#!/bin/bash
# Compatibility shim: claude-vm is now `dev-vm --claude`.
# Kept so existing muscle memory / docs / self-update keep working unchanged.
exec "$(dirname "${BASH_SOURCE[0]}")/dev-vm" --claude "$@"
